sudo: required
language: generic
before_install:
  - export TZ=America/New_York
  - sudo apt-get install texlive-binaries
script:
  - mkdir -p result
  - xelatex -synctex=1 -interaction=nonstopmode dissertation.tex
  - mv dissertation.pdf result/
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: false
  local-dir: result
  verbose: true
  on:
    branch: master
